<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HamStudyX.MainPage"
             BackgroundColor="{StaticResource PrimaryBackgroundColor}"
             Title="HamStudyX">

    <VerticalStackLayout Spacing="20" Padding="20">

        <!-- 1. Topic Selection -->
        <Label Text="Select a Topic:"
               FontSize="20" 
               FontAttributes="Bold" />

        <Picker x:Name="topicPicker"
                Title="Choose a topic:" />

        <!-- 2. Button to load the selected topic -->
        <Button Text="Load Quiz"
                Style="{StaticResource AccentButtonStyle}"
                Clicked="OnLoadQuizClicked" /> 

        <!-- 3. Label for displaying the current question prompt -->
        <Label x:Name="questionLabel"
               TextColor="{StaticResource PrimaryTextColor}"
               FontSize="25"
               FontAttributes="Bold"
               HorizontalOptions="CenterAndExpand" />

        <!-- 4. A stack where we add multiple-choice Buttons dynamically -->
        <VerticalStackLayout x:Name="optionsStack"
                             Spacing="10" />

        <!-- 5. Entry for typed answers if it's NOT multiple-choice -->
        <Entry x:Name="userAnswerEntry"
               Placeholder="Type your answer here"
               IsVisible="False" />

        <!-- 6. Next Button to move to the next question -->
        <Button x:Name="nextButton"
                Text="Next"
                Style="{StaticResource AccentButtonStyle}"
                Clicked="OnNextClicked"
                IsVisible="False" />

    </VerticalStackLayout>
</ContentPage>
